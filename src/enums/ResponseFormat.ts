/**
 * Enum representing the format of the response for facetted search and
 * related items API calls.
 * Currently, only 'detailed' format is supported.
 */
export enum ResponseFormat {
  Detailed = 'detailed',
}
